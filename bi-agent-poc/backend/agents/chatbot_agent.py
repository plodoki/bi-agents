class ChatBotAgent:
    def get_user_query(self, user_input: str) -> str:
        # Minimal example: just return the raw query
        # Real logic might call LLM to clarify or refine
        return user_input
